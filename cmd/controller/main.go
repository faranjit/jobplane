// Package main is the entry point for the jobplane controller.
// The controller is the "Brain" HTTP service that owns intent, validation,
// lifecycle, and persistence. It never executes jobs.
package main

func main() {
	// TODO: Initialize config, logger, database, and HTTP server
}
